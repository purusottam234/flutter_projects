import 'package:flutter/material.dart';

class MyStatelessLayoutWidget extends StatelessWidget {
  const MyStatelessLayoutWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        actions: [
          IconButton( 
            icon: icon)
        ]),
    );
  }
}